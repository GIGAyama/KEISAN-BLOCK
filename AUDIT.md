# 監査：さんすうブロック（KEISAN-BLOCK）

GIGA Standard v5 / Part III 改修モードによる現状監査。
**測定日：2026-08-04　測定者：Rollout Engineer**

- **型**：A型（単一 HTML 完結・ビルドなし・CDN 依存なし）
- **測り方**：静的読み取り → 実ブラウザ（Chromium 1366×768 / DPR 2）で全画面を走査
  → 実際に押して進行 → PWA の挙動を実測（§7-1 の 1〜4 をすべて実施）
- **測っていないもの**は、この文書のいちばん下にまとめて書いた。「未計測」は ✅ ではない。

---

## いちばん重要な3つ

| # | 何が起きているか | 実測 |
|---|---|---:|
| 1 | **色のついたボタンの上の白い文字が、ほぼ全部読めない。** ホームの6つのモードカード、ひきざん・はってんのさくせん選び、オーバーレイのボタン。児童が最初に見る面がまとめて基準未満 | **53件**（最悪 **1.43**） |
| 2 | **`sw.js` が `install` の中で `skipWaiting()` している。** 版を上げて3秒放置したら、児童が何も押していないのに新版へ入れ替わった。れんしゅう中に切り替わると、打ちかけの答えと並べたブロックが消える | `waiting: false` |
| 3 | **maskable アイコンが `icon-512.png` とバイト単位で同一。** `purpose:"maskable"` と書いてあるだけで中身は maskable ではなく、円で切り抜くと白いカードの四隅が欠ける | セーフゾーン外の中身 **1.35%**（目標 0.2%以下） |

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ❌ | ファイルなし |
| A2 | .gitignore | ❌ | ファイルなし |
| A3 | dependabot.yml | ❌ | ファイルなし（依存パッケージ自体は 0） |
| A4 | README.md / MANUAL.md / AUDIT.md | ⚠️ | README.md のみ（231行・充実）。MANUAL.md なし／AUDIT.md は本書で新設 |

`.clasp.json` / `.env` のコミットは **0件**（`git ls-files` で確認）。秘密情報の直書きも 0件。

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ❌ | `Content-Security-Policy` の指定なし |
| B2 | 秘密情報・IDの直書きなし | ✅ | APIキー・シートID・メールアドレス 0件 |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を使わない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` の使用 0件 |
| B5 | サーバー側5段ガード | — | 該当なし（サーバーを持たない。個人情報も持たない） |
| B6 | CDN から取る実行コードが 0 | ✅ | **0 バイト**。`@babel/standalone` / `cdn.tailwindcss.com` / unpkg / jsDelivr いずれも 0件 |
| B7 | 残る外部資産に SRI と版の固定 | — | 該当なし（外部資産 0件） |

> **B1 は入れやすい状態にある。** 本体の JavaScript は最初から 8本の外部ファイルに分かれていて、
> `index.html` にインラインの `<script>` も `onclick=` も無い（実測 0件）。
> §2-13 でいちばん壊れるとされた形に当てはまらないため、`'unsafe-inline'` なしで入れられる見込み。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし（GAS を使わない） |
| C2 | 自動復旧 | — | 該当なし |
| C3 | pagehide で記録確定 | ✅ | `js/studySession.js:73` で `pagehide` を購読。`beforeunload` を使わない理由もコメント済み |
| C4 | 通信失敗時のリトライと明示 | — | 該当なし（起動後に通信しない） |
| C5 | localStorage.clear() を使っていない | ✅ | 使用 0件。`resetAll()` は自アプリのキーのみ。`study.records.v1` を消さないことがコメントで明示されている |

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ⚠️ | `viewport-fit=cover` はある。**ただし `maximum-scale=1, user-scalable=no` が付いている**（`index.html:5`）→ D14 参照 |
| D2 | 100dvh を使用 | ⚠️ | `100vh` の単独使用は **0件**。`min-height:100dvh` は 1件のみで、`@supports` のフォールバックが無い |
| D3 | safe-area-inset を適用 | ⚠️ | 2件のみ（`#app` の四辺・トースト上端）。下部固定要素は無いので実害は小さい |
| D4 | clamp() による fluid type | ✅ | **57箇所**。文字・ブロック・キーパッドまで一貫して流体 |
| D5 | Canvas に DPR 補正 | — | 該当なし（`getContext('2d')` の使用 0件。図はすべて SVG と DOM） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **0件**（320×568 で home / sub-select / dev-select / free / records を実測） |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` 0件（アイコンはすべてインライン SVG）。PNG は最大 78.5KB |
| D8 | **コントラスト 4.5:1 以上** | ❌ | **53件が基準未満**（下表） |
| D9 | **タップ領域 44px 以上** | ❌ | **14件が 44px 未満**（下表） |
| D10 | prefers-reduced-motion 対応 | ⚠️ | ブロックに関する3行のみ。全体の `animation-duration` を抑える既定が無く、`forwards` を使うアニメが4件ある |
| D11 | forced-colors 対応 | ❌ | 指定 0件 |
| D12 | 提示モード | ❌ | 指定 0件。**このアプリは一斉授業で使われる**（「せつめいモード … みんなで かんがえる とき」が実装されている） |
| D13 | 印刷CSS | — | 該当なし（印刷して配るアプリではない） |
| D14 | **拡大を禁止していない** | ❌ | `user-scalable=no` / `maximum-scale=1` の両方が入っている。**見えづらい子が拡大できない** |

### D8 コントラスト基準未満（実ブラウザ 53件）— 悪い順

| 文言 | 場所 | 文字色 | 面 | 比 | 必要 |
|---|---|---|---|---:|---:|
| 「タイムアタック／あんざんで しょうぶ！」 | ホーム モードカード | 白 | `#ffd35c` | **1.43** | 3 / 4.5 |
| 「25＋8, 47＋6 …」 | はってん えらび | 白 | `#ffc586` | **1.54** | 4.5 |
| 「13−9 → 13−3=10 …」「33−6, 52−7 …」 | さくせん えらび | 白 | `#7de3a7` | **1.57** | 4.5 |
| 「たしざん／くりあがり」ほか | ホーム・はってん | 白 | `#ffb25e` | **1.78** | 3 / 4.5 |
| 「13−9 → 10−9=1 → 1+3=4」 | さくせん えらび | 白 | `#91c7ff` | **1.78** | 4.5 |
| 「はってん」「ひきざん」ほか | ホーム・さくせん | 白 | `#52d98a` | **1.80** | 3 / 4.5 |
| 「answer-display の `?`」 | れんしゅう（答え欄） | `#c6b8dd` | 白 | **1.86** | 3 |
| 「じぶんで しきを いれる」「ミックス」 | ホーム・さくせん | 白 | `#ff9ec4` | **1.91** | 3 / 4.5 |
| 「10の まとまり できた！」 | れんしゅう（緑タグ） | 白 | `#2ecc71` | **2.10** | 4.5 |
| 「◯＋◯の けいさんの しかた」 | せいかい オーバーレイ | 白 | `#2ecc71` | **2.10** | 4.5 |
| 「ひきざん／くりさがり」 | ホーム | 白 | `#6cb4ff` | **2.19** | 3 / 4.5 |
| 「さくらんぼの `?`」 | れんしゅう | `#aaaabb` | 白 | **2.29** | 3 |
| 「しきに書き込む 10」 | れんしゅう | `#f4701f` | `#cdeeff` | **2.40** | 3 |
| 「＋（えらんでいる側）」 | じぶんで しきを いれる | 白 | `#4aa3ff` | **2.63** | 3 |
| 「できたね！」「ゴール！」「5もん クリア！」 | 各オーバーレイ | `#e8862a` | 白 | **2.67** | 3 |
| 「タイム／まちがい／さいこうきろく」の数値 | けっか | `#e8862a` | 白 | **2.67** | 4.5 |
| 「きろく／マップ・カレンダー・バッジ」 | ホーム | 白 | `#b98ae0` | **2.70** | 3 / 4.5 |
| 「けす」 | キーパッド | `#d4576b` | `#ffe3e3` | **3.23** | 4.5 |
| 「じどうで うごかす」 | れんしゅう | `#2f7fd6` | `#eaf1fb` | **3.60** | 4.5 |
| 「れんぞく ◯にち」 | ホーム | `#c05e00` | `#fff1e2` | **3.91** | 4.5 |
| 「あと ◯こ」 | れんしゅう | `#2f7fd6` | 白 | **4.10** | 4.5 |
| 「きろくを ぜんぶ けす」 | きろく | `#c0392b` | `#ffd9d9` | **4.19** | 4.5 |
| 「ぜんぶ けす」 | キーパッド | `#67707e` | `#eef2f7` | **4.45** | 4.5 |

**形は §2-8 に書かれている通りだった。** 明るい色（面用）をそのまま文字の背に使い、
その上に白を載せている。面用と文字用の2段階が用意されていない。

**児童がいちばん先に見るホームの6枚が、6枚とも基準未満**である。

### D9 タップ44px未満（実ブラウザ 14件）

| 要素 | 実測 | 場所 |
|---|---:|---|
| きろくのタブ 5枚（せいせき／あしあと／マップ／カレンダー／バッジ） | 118.8 × **36** | きろく |
| 「＋」「−」 | 46 × **34** | じぶんで しきを いれる |
| 「じどうで うごかす」 | 147.9 × **34** | れんしゅう |
| 「きろくを ぜんぶ けす」 | 167.3 × **43** | きろく |

いずれも横は足りていて**縦だけが足りない**。§2-9 の通り、
`min-height` で背を伸ばすとタブが折り返すので、**疑似要素で当たり判定だけを広げる**のが正しい。

## E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ❌ | `id` が**無い**。`scope` / `start_url` は `"./"`。同一オリジンに数十個ある中で取り違えが起きうる |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ⚠️ | 192 / 512 / maskable-512 の**3種**（maskable-192 なし）。apple-touch-icon は `icon-192.png` を流用しているが**完全不透明**なので四隅が黒くなる事故は起きない（α値 min=255 を実測） |
| E3 | beforeinstallprompt を head 最上部で捕捉 | ❌ | 捕捉は `js/main.js:1759`。`main.js` は `</body>` 直前の**8本目**のスクリプト。通信が遅い端末で合図を取りこぼす |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | `#btn-install` は既定 `hidden`、`beforeinstallprompt` で表示、`appinstalled` で非表示 |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | **実測で確認**。別名キャッシュ2つ（`townmap-mikke-static-v1` / `haiku-meeting-static-v3`）を置いてから版を上げ、**2つとも残った** |
| E6 | sw.js が localStorage に触れていない | ✅ | 使用 0件 |
| E7 | 更新通知（押すまで切り替わらない） | ❌ | **`install` の中で `skipWaiting()`（`sw.js:26`）**。版を上げて `update()` → **3秒放置で `waiting: false`**、active が新版に置き換わっていた。更新を知らせるトーストも無い |
| E8 | 初回訪問で勝手にリロードしない | ✅ | **画面遷移 1回**（まっさらな状態で `framenavigated` を計数）。`controllerchange` を購読していないため |
| E9 | Service Worker が実際に登録されている | ✅ | `getRegistration()` が登録を返す。`controller` も付く |
| E10 | offline.html | ❌ | ファイルが無い。`fetch` ハンドラにも画面遷移用のフォールバックが無い |
| E11 | APP_VERSION を今回のリリース値に更新した | ⚠️ | `CACHE = "sansu-block-v8"`。仕組みはあるが、今回の改修に合わせて上げる必要がある |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ❌ | **1.35%**。`icon-maskable-512.png` は `icon-512.png` と **SHA256 が一致**（同一ファイル）。白いカードの四隅が円マスクで欠ける |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ❌ | MANUAL.md が無い |

### 実測した PWA の挙動（§7-5 の表）

| 確かめたこと | 結果 |
|---|---|
| 登録されているか | ✅ 登録あり |
| 初回に勝手にリロードしないか | ✅ **画面遷移 1回** |
| 押すまで切り替わらないか | ❌ **3秒放置で切り替わった**（`waiting: false`／`skipWaiting()` のため） |
| 押したら切り替わるか | — 押すボタンが無い |
| 他アプリを巻き添えにしないか | ✅ **2つとも残った** |
| 圏外で起動するか | ✅ 起動する |
| offline.html が出るか | ❌ ファイルが無い |

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ⚠️ | `<img>` は 0件。アイコンボタンに `aria-label` あり・中の SVG に `aria-hidden` あり（良い）。`aria-live` は 4箇所。**`role="alert"` は 0件** |
| F2 | モーダルに role="dialog"・Esc で閉じる | ❌ | 5つのオーバーレイに `role="dialog"` / `aria-modal` が無い。Esc の処理も無く、フォーカスも閉じ込めていない |
| F3 | キーボードのみで全機能に到達 | ⚠️ | 数字・Backspace・Enter・＋−のキー操作は実装済み（`main.js:2040`）。ただしオーバーレイ表示中に背後の要素へ Tab が抜ける |
| F4 | rt の色を決め打ちしていない | — | 該当なし（`<ruby>` / `<rt>` の使用 0件。本文がすべてひらがな） |
| F5 | 初回JS 300KB以下 | ✅ | **136.0 KB**（8ファイル合計・gzip前） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `js/main.js` の **2,093行 / 80.3KB**、`css/style.css` が **2,100行 / 45.7KB** |
| — | 総アセット（初回） | ✅ | **374 KB**（目標 1MB 以下） |

見出しは `<h1>` が無く `<h2>` から始まっている（`h2`×4 / `h3`×3）。

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | `js/studyLog.js` が参照実装（ロジック版 1.1）。氏名・出席番号・メールを持たず、外部送信もしない |
| G2 | 中断記録・5分ルール | ✅ | `pagehide` で確定、`visibilitychange` / `pageshow(persisted)` で再開。5分ルールは `js/studySession.js` に実装 |

---

## 対応フェーズの割り当て

| フェーズ | 内容 | 該当 |
|---|---|---|
| **P0** | LICENSE / .gitignore / dependabot.yml | A1・A2・A3 |
| **P0.5** | 依存 | **該当なし**（CDN 実行コード 0バイト。この形はすでに正しい） |
| **P1** | 拡大禁止の解除 → dvh フォールバック → safe-area → タップ44px → reduced-motion / forced-colors → **コントラスト53件** → PWA一式（manifest の id、install-hook、sw.js、offline.html、maskable）→ **CSP** | D1・D2・D3・D8・D9・D10・D11・D14・E1・E3・E7・E10・E11・E12・B1 |
| **P2** | maskable の作り直しとセーフゾーンの画素確認 | E12 |
| **P3** | MANUAL.md 作成、README に不足節を追記、オーバーレイの `role="dialog"` / Esc | A4・E13・F1・F2・F3 |
| **P4** | 品質ゲート（`scripts/check-project.mjs`）＋ CI を `pull_request` でも動かす | — |

**D12（提示モード）は人間に確認する。** 画面を足す変更であり、
「一斉授業で使うなら必ず用意する」に当てはまる一方で、機能追加にあたる。

---

## 測っていないもの（「未計測」は ✅ ではない）

- **本番（`gigayama.github.io`）での表示・インストール・更新**
  … 作業環境からプロキシ 403 で到達できない。すべて手元の静的サーバ（127.0.0.1）での実測である
- **実機の Chromebook / iPad / 電子黒板での見え方**
  … Chromium（DPR 2・1366×768 と 320×568）でのみ測った。液晶の視野角・実際の輝度は測っていない
- **`beforeinstallprompt` を取りこぼすかどうか**
  … headless Chromium では発火しない。E3 は「捕捉位置が `</body>` 直前の8本目である」という
  構造の指摘であって、取りこぼしを実測したものではない
- **iOS Safari の挙動**（ITP による localStorage の失効・ホーム画面追加）
  … WebKit を用意していない
- **音の再生**（`js/audio.js` の WebAudio）… headless では鳴らせない
- **LCP / CLS の実測値**
  … Chromebook 実機でないと意味のある数字にならないため測っていない。
  初回 JS 136KB・総アセット 374KB・`<img>` 0件・Web フォント 0件という構成から
  目標を外す要素は見当たらないが、**数字としては未計測**である
- **「改修前は壊れていた」という言い方はしていない。** 上の数字はすべて
  現在の `main`（`d2e5817`）をそのままビルドせず開いて測ったものである

---

## 計測器そのものについて（他リポジトリにも効く）

§7-2 のコントラスト計測のひな形には、`getImageData` の結果を α で割る行がある。

```javascript
return a === 0 ? [0,0,0,0] : [d[0]/a, d[1]/a, d[2]/a, a];   // ← これ
```

**Chrome の `getImageData` は「非」乗算済みの値を返す。**
実測すると `rgba(255,255,255,0.22)` は `[255, 255, 255, 56]` で返る。
ここで α（0.22）で割ると **1159** になり、範囲外の色ができあがる。

このリポジトリでは、半透明の白を敷いた `.menu-example`（例：`13−9 → 10−9=1 → 1+3=4`）で
**比が 2.48 と出た。割り算を外すと実際は 1.78 だった。**
半透明の面に載った文字を、実際より 0.7 も良く見積もることになる。

| 対象 | 割り算あり（誤） | 割り算なし（正） |
|---|---:|---:|
| `.menu-example`（青のカード） | 2.48 | **1.78** |
| `.menu-example`（緑のカード） | 2.65 | **1.57** |
| `.menu-example`（橙のカード） | 2.69 | **1.54** |

**半透明の面は、教育アプリのカードでよく使われる形である。**
他のリポジトリを同じひな形で測っていた場合、そこだけ甘く出ている可能性がある。
