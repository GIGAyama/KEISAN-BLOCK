# 監査：さんすうブロック（KEISAN-BLOCK）

GIGA Standard v5 / Part III 改修モードによる監査。
**測定日：2026-08-04　測定者：Rollout Engineer**

> **この文書は改修後の状態を書いている。**
> 「前」は改修前（`d2e5817`）を、「後」は改修後を、
> どちらも同じ計測器で実ブラウザにかけた実測値である。
> 推測で書いた欄はない。測っていないものは末尾にまとめた。

- **型**：A型（単一 HTML 完結・ビルドなし・CDN 依存なし）
- **測り方**：静的読み取り → 実ブラウザ（Chromium 1366×768 / DPR 2）で全画面を走査
  → 実際に押して進行 → PWA の挙動を実測（§7-1 の 1〜4 をすべて実施）
- **測っていないもの**は、この文書のいちばん下にまとめて書いた。「未計測」は ✅ ではない。

---

## いちばん重要だった3つ（すべて解消）

| # | 何が起きていたか | 前 | 後 |
|---|---|---:|---:|
| 1 | **色のついたボタンの上の白い文字が、ほぼ全部読めなかった。** 児童が最初に見るホームの6枚が、6枚とも基準未満 | **53件**（最悪 **1.43**） | **0件** |
| 2 | **`sw.js` が `install` の中で `skipWaiting()`。** 版を上げて3秒放置したら、誰も押していないのに新版へ入れ替わった | `waiting: false` | `waiting: true`（押すまで待つ） |
| 3 | **maskable アイコンが `icon-512.png` と SHA256 まで同一。** 円で切り抜くと白いカードの四隅が欠ける | **1.35%** | **0.000%** |

## 全体の実測（前 → 後）

| 項目 | 前 | 後 |
|---|---:|---:|
| コントラスト基準未満 | **53件** | **0件** |
| タップ領域 44px 未満 | **14件** | **0件** |
| CSP 違反 | ―（CSP 無し） | **0件** |
| JS エラー | 0件 | **0件** |
| 320px での横スクロール | 0件 | **0件** |
| 初回訪問の画面遷移 | 1回 | **1回** |
| 版を上げて3秒放置 | 勝手に切り替わった | **待機のまま** |
| 「さいしんに する」を押す | ―（ボタン無し） | **切り替わって再読み込み** |
| 他アプリのキャッシュ | 残る | **残る** |
| offline.html | 無い | **出る**（本体を消して圏外で確認） |
| maskable セーフゾーン外の中身 | 1.35% | **0.000%** |
| アイコン合計 | 173.2 KB | **53.0 KB** |
| 初回 JS（gzip前） | 136.0 KB | **145.8 KB** |
| 総アセット（初回） | 374 KB | **286 KB** |
| 品質ゲート | 無し | **0 error / 0 warning**（22件の故意の破壊で発火を確認） |
| テスト | 無し | **7件** |

> **タップとコントラストの「後」の 0件は、前より広い範囲を測った上での 0件である。**
> 前の走査では、データが無くて描画されない画面（あしあと・バッジ）、
> `hidden` のままの UI（さくせん選び・こたえを見る・けいさんの しかた）、
> トースト、更新のおしらせ、提示モード、1920px を測れていなかった。
> 後は localStorage に記録を仕込み、`hidden` を外し、提示モードでも走らせている。
> **その広げた走査で、前の走査では見えなかったタップ3件が新たに見つかり、直してある。**

---

## A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ✅ | `node_modules/` `dist/` `.clasp.json` `.env` ほか |
| A3 | dependabot.yml | ✅ | github-actions を monthly。npm の依存は 0 のため対象にしない |
| A4 | README.md / MANUAL.md / AUDIT.md | ✅ | 3つとも有り |

`.clasp.json` / `.env` のコミットは **0件**（`git ls-files` で確認）。秘密情報の直書きも 0件。

## B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ✅ | `'self'` のみ。`'unsafe-inline'` なし。**入れたうえで全画面を歩き CSP違反 0件・JSエラー 0件**。`addStyleTag` が実際に拒否されることも確認 |
| B2 | 秘密情報・IDの直書きなし | ✅ | APIキー・シートID・メールアドレス 0件 |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を使わない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` の使用 0件 |
| B5 | サーバー側5段ガード | — | 該当なし（サーバーを持たない。個人情報も持たない） |
| B6 | CDN から取る実行コードが 0 | ✅ | **0 バイト**。`@babel/standalone` / `cdn.tailwindcss.com` / unpkg / jsDelivr いずれも 0件 |
| B7 | 残る外部資産に SRI と版の固定 | — | 該当なし（外部資産 0件） |

> **B1 が入れやすかった理由。** 本体の JavaScript は最初から 8本の外部ファイルに分かれていて、
> `index.html` にじかに書いたスクリプトも `onclick=` も無かった（実測 0件）。
> §2-13 でいちばん壊れるとされた形に当てはまらず、`'unsafe-inline'` なしで締められた。
>
> 入れてから引っかかったのは **2箇所だけ**で、どちらも動かして初めて分かるものだった。
>
> | 引っかかったもの | 直し方 |
> |---|---|
> | `index.html` の `style="display:none"`（SVG定義） | CSS へ移した |
> | あしあとの棒グラフが `innerHTML` に `style="width:..."` を書いていた | 要素の `.style` へ代入する形に |
>
> **後者はビルドも静的解析も通る。** 「あしあと」タブに記録がある状態で開かないと、
> 棒が伸びないことに気づけない。

## C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし（GAS を使わない） |
| C2 | 自動復旧 | — | 該当なし |
| C3 | pagehide で記録確定 | ✅ | `js/studySession.js:73` で `pagehide` を購読。`beforeunload` を使わない理由もコメント済み |
| C4 | 通信失敗時のリトライと明示 | — | 該当なし（起動後に通信しない） |
| C5 | localStorage.clear() を使っていない | ✅ | 使用 0件。`resetAll()` は自アプリのキーのみ。`study.records.v1` を消さないことがコメントで明示されている |

## D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | `width=device-width, initial-scale=1, viewport-fit=cover` |
| D2 | 100dvh を使用 | ✅ | `100vh` の単独使用 0件。`@supports not (min-height: 100dvh)` のフォールバックを追加 |
| D3 | safe-area-inset を適用 | ✅ | `--safe-t/b/l/r` に変数化。下部固定の更新バーにも適用 |
| D4 | clamp() による fluid type | ✅ | **57箇所**。文字・ブロック・キーパッドまで一貫して流体 |
| D5 | Canvas に DPR 補正 | — | 該当なし（`getContext('2d')` の使用 0件。図はすべて SVG と DOM） |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **0件**（320×568 で home / sub-select / dev-select / free / records を実測） |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` 0件（アイコンはすべてインライン SVG）。PNG は最大 **16.2KB**（前は 78.5KB） |
| D8 | **コントラスト 4.5:1 以上** | ✅ | **0件**（下表の53件をすべて解消。提示モード・1920px でも 0件） |
| D9 | **タップ領域 44px 以上** | ✅ | **0件**。疑似要素で当たり判定だけ拡張。見た目の3px外を押してタブが切り替わることを実測 |
| D10 | prefers-reduced-motion 対応 | ✅ | 全体に `.01ms`（`0` ではない）。紙ふぶきは非表示 |
| D11 | forced-colors 対応 | ✅ | ボタン・タブ・スロット・更新バーに枠 |
| D12 | 提示モード | ✅ | 「大きく表示」で文字150%・ボタン64px・式を大きく＋フルスクリーン。設定は保存される |
| D13 | 印刷CSS | — | 該当なし（印刷して配るアプリではない） |
| D14 | **拡大を禁止していない** | ✅ | 両方とも外した |

### D8 改修前に基準未満だった 53件（悪い順）

**すべて 4.5:1 以上（大きな文字は 3:1 以上）に到達している。**

| 文言 | 場所 | 文字色 | 面 | 比 | 必要 |
|---|---|---|---|---:|---:|
| 「タイムアタック／あんざんで しょうぶ！」 | ホーム モードカード | 白 | `#ffd35c` | **1.43** | 3 / 4.5 |
| 「25＋8, 47＋6 …」 | はってん えらび | 白 | `#ffc586` | **1.54** | 4.5 |
| 「13−9 → 13−3=10 …」「33−6, 52−7 …」 | さくせん えらび | 白 | `#7de3a7` | **1.57** | 4.5 |
| 「たしざん／くりあがり」ほか | ホーム・はってん | 白 | `#ffb25e` | **1.78** | 3 / 4.5 |
| 「13−9 → 10−9=1 → 1+3=4」 | さくせん えらび | 白 | `#91c7ff` | **1.78** | 4.5 |
| 「はってん」「ひきざん」ほか | ホーム・さくせん | 白 | `#52d98a` | **1.80** | 3 / 4.5 |
| 「answer-display の `?`」 | れんしゅう（答え欄） | `#c6b8dd` | 白 | **1.86** | 3 |
| 「じぶんで しきを いれる」「ミックス」 | ホーム・さくせん | 白 | `#ff9ec4` | **1.91** | 3 / 4.5 |
| 「10の まとまり できた！」 | れんしゅう（緑タグ） | 白 | `#2ecc71` | **2.10** | 4.5 |
| 「◯＋◯の けいさんの しかた」 | せいかい オーバーレイ | 白 | `#2ecc71` | **2.10** | 4.5 |
| 「ひきざん／くりさがり」 | ホーム | 白 | `#6cb4ff` | **2.19** | 3 / 4.5 |
| 「さくらんぼの `?`」 | れんしゅう | `#aaaabb` | 白 | **2.29** | 3 |
| 「しきに書き込む 10」 | れんしゅう | `#f4701f` | `#cdeeff` | **2.40** | 3 |
| 「＋（えらんでいる側）」 | じぶんで しきを いれる | 白 | `#4aa3ff` | **2.63** | 3 |
| 「できたね！」「ゴール！」「5もん クリア！」 | 各オーバーレイ | `#e8862a` | 白 | **2.67** | 3 |
| 「タイム／まちがい／さいこうきろく」の数値 | けっか | `#e8862a` | 白 | **2.67** | 4.5 |
| 「きろく／マップ・カレンダー・バッジ」 | ホーム | 白 | `#b98ae0` | **2.70** | 3 / 4.5 |
| 「けす」 | キーパッド | `#d4576b` | `#ffe3e3` | **3.23** | 4.5 |
| 「じどうで うごかす」 | れんしゅう | `#2f7fd6` | `#eaf1fb` | **3.60** | 4.5 |
| 「れんぞく ◯にち」 | ホーム | `#c05e00` | `#fff1e2` | **3.91** | 4.5 |
| 「あと ◯こ」 | れんしゅう | `#2f7fd6` | 白 | **4.10** | 4.5 |
| 「きろくを ぜんぶ けす」 | きろく | `#c0392b` | `#ffd9d9` | **4.19** | 4.5 |
| 「ぜんぶ けす」 | キーパッド | `#67707e` | `#eef2f7` | **4.45** | 4.5 |

**形は §2-8 に書かれている通りだった。** 明るい色（面用）をそのまま文字の背に使い、
その上に白を載せていた。面用と文字用の2段階が用意されていなかった。

**児童がいちばん先に見るホームの6枚が、6枚とも基準未満**だった。

#### 面の置き換え（色相は変えていない）

| カード | 前（上→下） | 後（上→下） | 白ぬきの比 |
|---|---|---|---:|
| たしざん | `#ffb25e` → `#e8862a` | `#b15c00` → `#8d4c0f` | 1.78 → **4.77** |
| ひきざん | `#6cb4ff` → `#2f7fd6` | `#0070e4` → `#205ea2` | 2.19 → **4.73** |
| タイムアタック | `#ffd35c` → `#e8a10e` | `#946c00` → `#7c5607` | 1.43 → **4.77** |
| はってん | `#52d98a` → `#1faa58` | `#1e8349` → `#136b37` | 1.80 → **4.77** |
| じぶんで しきを いれる | `#ff9ec4` → `#e05a92` | `#e30059` → `#af215c` | 1.92 → **4.76** |
| きろく | `#b98ae0` → `#8a53c4` | `#9751d1` → `#793eb6` | 2.70 → **4.76** |

**見た目ははっきり変わる。** 色相は変えていないが、面はどれも濃くなった。

アイコンだけ元の明るい色に戻して色の見分けを保てないか試したが、
**6枚中4枚で図形のコントラストが 3:1 に届かない**（きろく 1.76 / ひきざん 2.17 /
じぶんで 2.48 / はってん 2.65）。測ったうえでやめた。
カードは名前・説明・アイコンの形でも区別できるので、色は4つ目の手がかりである。

れいの ふだ（`.menu-example`）は白を25%重ねていた。**面が薄くなり、
白ぬき文字にとっていちばん読めない場所**になっていたので、黒22%に変えた。
`.mode-desc` / `.menu-small` の `opacity: 0.92` も外した。薄いぶんだけ読みにくいだけだった。

### D9 44px に届いていなかった 17件

| 要素 | 前 | 直し方 |
|---|---:|---|
| きろくのタブ 5枚 | 118.8 × **36** | 疑似要素 |
| 「＋」「−」 | 46 × **34** | 疑似要素（すきまを 6px → 12px に広げて重なりを避けた） |
| 「じどうで うごかす」 | 147.9 × **34** | 疑似要素 |
| 「10から ひいて たす」「ばらから ひいて…」 | 298 × **40.2** | 疑似要素 |
| 「きろくを ぜんぶ けす」 | 167.3 × **43** | `min-height: 44px` |
| 「アプリを インストール」 | 223.2 × **43** | `min-height: 44px` |

いずれも横は足りていて**縦だけが足りなかった**。§2-9 の通り、
`min-height` で背を伸ばすとタブが折り返すので、**疑似要素で当たり判定だけを広げた**。

> **下の4件（さくせん選び2つ・インストールボタン）は、最初の走査では見えなかった。**
> `hidden` が付いたままの UI と、条件が揃わないと出ないボタンだったためである。
> データを仕込み `hidden` を外して測り直して、はじめて出てきた。

**当たり判定が本当に効いているかも測った。** きろくのタブの
**見た目の 3px 外側**をクリックすると、マップタブに切り替わる。
「`::after` が 44px と報告される」だけでは、押せる保証にならない。

## E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | 3つとも `/KEISAN-BLOCK/`。`"./"` の解決結果と同じなので、既にインストール済みの端末で別アプリ扱いにはならない |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | 192 / 512 / maskable-192 / maskable-512 の4種＋専用 apple-touch-icon（180・完全不透明） |
| E3 | beforeinstallprompt を head 最上部で捕捉 | ✅ | `install-hook.js` を `<head>` 先頭で同期読み込み。本体は `window.__pwaInstallPrompt` を見るだけ |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | `#btn-install` は既定 `hidden`、`beforeinstallprompt` で表示、`appinstalled` で非表示 |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | **実測で確認**。別名キャッシュ2つ（`townmap-mikke-static-v1` / `haiku-meeting-static-v3`）を置いてから版を上げ、**2つとも残った** |
| E6 | sw.js が localStorage に触れていない | ✅ | 使用 0件 |
| E7 | 更新通知（押すまで切り替わらない） | ✅ | `install` の `skipWaiting()` を削除。版を上げて `update()` → **3秒放置で `waiting: true`・リロード 0回**。押すと切り替わって再読み込み |
| E8 | 初回訪問で勝手にリロードしない | ✅ | **画面遷移 1回**。改修で `controllerchange` を購読するようになったが、**利用者が押したときだけ**受けるため 1回のまま |
| E9 | Service Worker が実際に登録されている | ✅ | `getRegistration()` が登録を返す。`controller` も付く |
| E10 | offline.html | ✅ | 追加。画面遷移は network-first → `index.html` → `offline.html`。**サーバを止めて実測** |
| E11 | APP_VERSION を今回のリリース値に更新した | ✅ | `APP_VERSION = "v9"` |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | **0.000%**（192 / 512 とも）。下地を端まで伸ばし、中身を縮めた |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | MANUAL.md §1。**Safari 以外では追加できない**ことも書いた |

### 実測した PWA の挙動（§7-5 の表）

| 確かめたこと | 結果 |
|---|---|
| 登録されているか | ✅ 登録あり |
| 初回に勝手にリロードしないか | ✅ **画面遷移 1回** |
| 押すまで切り替わらないか | ❌ **3秒放置で切り替わった**（`waiting: false`／`skipWaiting()` のため） |
| 押したら切り替わるか | — 押すボタンが無い |
| 他アプリを巻き添えにしないか | ✅ **2つとも残った** |
| 圏外で起動するか | ✅ 起動する |
| offline.html が出るか | ❌ ファイルが無い |

## F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | `#feedback` に `role="alert"` を追加 |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | 5つとも `role="dialog" aria-modal="true"`。背後は `inert`。Esc は既存の「すすむ／ホームへ」に接続 |
| F3 | キーボードのみで全機能に到達 | ✅ | Tab 順が視覚順と一致。Enter で練習開始、数字キー＋Enter で解答。オーバーレイ中の Tab は中に閉じ込め（実測） |
| F4 | rt の色を決め打ちしていない | — | 該当なし（`<ruby>` / `<rt>` の使用 0件。本文がすべてひらがな） |
| F5 | 初回JS 300KB以下 | ✅ | **145.8 KB**（9ファイル合計・gzip前） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `css/style.css` の **2,306行**、`js/main.js` が **2,294行** |
| — | 総アセット（初回） | ✅ | **286 KB**（目標 1MB 以下） |

見出しは `<h1>` が無く `<h2>` から始まっていた（`h2`×4 / `h3`×3）。
画面は一度に1つしか出ないので、各画面の見出しを `h1`、画面内の小見出しを `h2` にそろえた。

## G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | `js/studyLog.js` が参照実装（ロジック版 1.1）。氏名・出席番号・メールを持たず、外部送信もしない |
| G2 | 中断記録・5分ルール | ✅ | `pagehide` で確定、`visibilitychange` / `pageshow(persisted)` で再開。5分ルールは `js/studySession.js` に実装 |

---

## 対応フェーズ（すべて実施ずみ）

| フェーズ | 内容 | 該当 | 結果 |
|---|---|---|---|
| **P0** | LICENSE / .gitignore / dependabot.yml | A1・A2・A3 | 完了 |
| **P0.5** | 依存 | — | **作業なし**。CDN からとる実行コードは元から **0 バイト**。`@babel/standalone` も Tailwind CDN も unpkg も jsDelivr も 0件で、この形はすでに正しかった |
| **P1** | 拡大禁止の解除／dvh フォールバック／safe-area／タップ44px／reduced-motion・forced-colors／**コントラスト53件**／PWA一式／**CSP**／提示モード | D1・D2・D3・D8〜D12・D14・E1〜E3・E7・E10〜E12・B1 | 完了 |
| **P2** | 性能 | E12・D7 | maskable の作りなおしとパレット化で **173.2KB → 53.0KB**。150KB 超の画像は元から 0件、`<img>` も 0件（すべてインライン SVG）なので、`width`/`height` の付与は**対象なし** |
| **P3** | MANUAL.md／README 追記／オーバーレイの `role="dialog"`・Esc・フォーカス閉じこめ／見出し階層 | A4・E13・F1・F2・F3 | 完了。**巨大ファイルの分割は行っていない**（最大 2,306行 / 45.7KB で上限に遠い） |
| **P4** | 品質ゲート＋テスト＋CI を `pull_request` でも | — | 完了。**22件の故意の破壊で発火を確認** |

**D12（提示モード）は人間の合意を得てから実装した。**

---

## 測っていないもの（「未計測」は ✅ ではない）

- **本番（`gigayama.github.io`）での表示・インストール・更新**
  … 作業環境からプロキシ 403 で到達できない。すべて手元の静的サーバ（127.0.0.1）での実測である
- **実機の Chromebook / iPad / 電子黒板での見え方**
  … Chromium（DPR 2・1366×768 と 320×568）でのみ測った。液晶の視野角・実際の輝度は測っていない
- **`beforeinstallprompt` を取りこぼすかどうか**
  … headless Chromium では発火しない。E3 の「捕捉が `</body>` 直前の8本目にあった」は
  構造の指摘であって、取りこぼしを実測したものではない。
  **`install-hook.js` が `<head>` の先頭で読まれ、リスナーを張ることは実測した**が、
  「遅い端末で実際に取りこぼしが減る」ことは測っていない
- **実際のインストール（ホーム画面への追加）とインストール後の起動**
  … headless では最後まで進められない
- **提示モードの `requestFullscreen`**
  … headless では利用者操作としてのフルスクリーンが取れない。
  `.presentation` が付いて文字とボタンが大きくなることは実測したが、
  **全画面になるかどうかは未計測**である
- **iOS Safari の挙動**（ITP による localStorage の失効・ホーム画面追加）
  … WebKit を用意していない
- **音の再生**（`js/audio.js` の WebAudio）… headless では鳴らせない
- **LCP / CLS の実測値**
  … Chromebook 実機でないと意味のある数字にならないため測っていない。
  初回 JS 145.8KB・総アセット 286KB・`<img>` 0件・Web フォント 0件という構成から
  目標を外す要素は見当たらないが、**数字としては未計測**である
- **`prefers-reduced-motion` と `forced-colors` の見た目**
  … CSS が当たることは確認したが、その状態で全画面を目で見てはいない
- **「改修前は壊れていた」と書いた箇所は、すべて実際に測っている。**
  この文書の「前」の欄は、改修前の `d2e5817` を静的サーバに載せて
  同じ計測器にかけた値である。推測で書いた欄はない

---

## 計測器そのものについて（他リポジトリにも効く）

§7-2 のコントラスト計測のひな形には、`getImageData` の結果を α で割る行がある。

```javascript
return a === 0 ? [0,0,0,0] : [d[0]/a, d[1]/a, d[2]/a, a];   // ← これ
```

**Chrome の `getImageData` は「非」乗算済みの値を返す。**
実測すると `rgba(255,255,255,0.22)` は `[255, 255, 255, 56]` で返る。
ここで α（0.22）で割ると **1159** になり、範囲外の色ができあがる。

このリポジトリでは、半透明の白を敷いた `.menu-example`（例：`13−9 → 10−9=1 → 1+3=4`）で
**比が 2.48 と出た。割り算を外すと実際は 1.78 だった。**
半透明の面に載った文字を、実際より 0.7 も良く見積もることになる。

| 対象 | 割り算あり（誤） | 割り算なし（正） |
|---|---:|---:|
| `.menu-example`（青のカード） | 2.48 | **1.78** |
| `.menu-example`（緑のカード） | 2.65 | **1.57** |
| `.menu-example`（橙のカード） | 2.69 | **1.54** |

**半透明の面は、教育アプリのカードでよく使われる形である。**
他のリポジトリを同じひな形で測っていた場合、そこだけ甘く出ている可能性がある。


---

## 計測のしかたで、もう2つ分かったこと

### `context.setOffline(true)` は Service Worker の中の `fetch` に効いていない

§7-5 は圏外の確認に `context.setOffline(true)` を挙げているが、
**この Chromium では Service Worker が出す `fetch` はそのまま外へ出る。**

確かめ方は単純である。キャッシュから `index.html` と `/` を消したうえで
`setOffline(true)` にして開くと、**アプリがそのまま出た**。
キャッシュに無いのに出るということは、ネットワークから取れている。

| やり方 | 本体をキャッシュから消して圏外にしたとき |
|---|---|
| `context.setOffline(true)` | **アプリが出る**（＝圏外になっていない） |
| HTTP サーバを実際に止める | `offline.html` が出る |

**`setOffline` だけで測ると、`offline.html` が無くても「出た」ことにできてしまう。**
実際、改修前のこのリポジトリには `offline.html` が存在しなかったのに、
`setOffline` での確認は素通りした。

圏外まわりは、**サーバを止めて測ること。**

### 品質ゲートの共通検査に 3つの不具合があった

`scripts/lib/giga-v4-checks.mjs` をこのリポジトリに当てて分かったもの。
**フリート共通のファイルなので、正本にも反映が必要である。**

| 不具合 | 中身 | 影響 |
|---|---|---|
| `src/` 決めうち | `walk(path.join(rootDir, 'src'))`。A型は `js/` `css/` を直下に置くので**1バイトも読まれない** | `SAFE_AREA` `REDUCED_MOTION` `LOCALSTORAGE_CLEAR` `FLUID_TYPE` がまとめて空振り。しかも「0件」と出る |
| モーダルの検出が Tailwind 決めうち | `"fixed inset-0"` だけを探す。素の CSS の `position: fixed; inset: 0` に当たらない | `A11Y_DIALOG` `A11Y_ESCAPE` が素通り。**このリポジトリはオーバーレイが5つあったのに 0件だった** |
| 判定前にコメントを落としていない | §P4 は `SW_LOCALSTORAGE` の誤検知を挙げているが、同じ形が3箇所に残っていた | 下表 |

コメントの件は、**誤検知だけでなく見のがしの向きにも出た**のがこわい。

| コード | 向き | 実際に起きたこと |
|---|---|---|
| `LOCALSTORAGE_CLEAR` | 誤検知 | 「`localStorage.clear()` も つかわない」という JSDoc に反応（`js/storage.js:228`） |
| `CSP_INLINE_SCRIPT` | 誤検知 | HTML コメントの中の script 開きタグを数えていた |
| `PWA_INSTALL` | **見のがし** | `index.html` のコメントに `beforeinstallprompt` と書いてあるだけで通る。**捕捉をまるごと消しても 0件と言った** |

**注意書きをちゃんと書いたリポジトリほど落ちる**という、さかさまな状態になっていた。
「コードがそうしているか」を見る検査は、コメントを落とした本文を見るようにそろえてある。

### 正本（`project-quality.mjs`）側にも 1つ残っている

`extractScriptBlocks` が **HTML コメントを落とさない**。
コメントの中に script の開きタグを書くと、そこから次の閉じタグまでを
JavaScript とみなして構文検査し、`HTML_SCRIPT_SYNTAX` / `ASSEMBLED_APP_SYNTAX` で落ちる。

`scripts/lib/project-quality.mjs` は
**正本の更新を丸ごと差し替えで受けられるようバイト単位のコピーのまま**にしてあるので、
このリポジトリでは `index.html` のコメントの書き方を変えて避けた（同ファイルにその旨を明記）。
**正本の側で直すべきものである。**
