# ROLLOUT.md — GIGA Standard v5 ロールアウトの記録

このファイルは **KEISAN-BLOCK 1本の記録ではない。**
ここで見つかったもののうち、**他のリポジトリにも効くもの**を残す場所である。
v5 §バッチ運用の「1本を深く終えたら、そこで見つかった不具合を
その1本の問題で終わらせない」に対応する。

---

## このリポジトリ（KEISAN-BLOCK / A型）

| フェーズ | 結果 |
|---|---|
| P0 法務 | LICENSE / .gitignore / dependabot.yml を追加 |
| P0.5 依存 | **作業なし**。CDN からとる実行コードは元から 0 バイト |
| P1 表示・PWA・CSP | コントラスト 53→0件、タップ 17→0件、CSP 投入、更新通知、offline.html、maskable 作りなおし、提示モード |
| P2 性能 | アイコン 173.2KB → 53.0KB、総アセット 374KB → 286KB |
| P3 保守性 | MANUAL.md、README 追記、モーダルの a11y、見出し階層 |
| P4 品質ゲート | 正本をコピー＋Part I 検査。**22件の故意の破壊で発火を確認** |

数字は `AUDIT.md` にある。

---

## 横断で確かめるべきこと（他リポジトリ）

### 1. 🔴 `context.setOffline(true)` では圏外を測れていない

**§7-5 の表の「圏外で起動するか」「offline.html が出るか」は、
`setOffline` だけで測っていると意味を持たない。**

この Chromium では、**Service Worker が出す `fetch` は `setOffline` を素通りする。**
ページ側の通信は止まるが、SW の中の `fetch(req)` は外に出てしまう。

| 測り方 | 本体をキャッシュから消して圏外にしたとき |
|---|---|
| `context.setOffline(true)` | **アプリが出る**（＝圏外になっていない） |
| HTTP サーバを実際に止める | `offline.html` が出る |

このリポジトリには `offline.html` が**存在しなかった**のに、
`setOffline` での確認は素通りした。

**すでに「オフライン確認ずみ」と書いた リポジトリは、測り直しが要る。**

```bash
# 影響するのは「offline.html を確認した」と書いてある全リポジトリ
grep -rln "setOffline" .
```

### 2. 🔴 §7-2 のコントラスト計測のひな形は、半透明の面で甘く出る

ひな形にある「`getImageData` の結果を α で割る」行が誤り。
**Chrome の `getImageData` は非乗算済みの値を返す。**

```javascript
// ❌ ひな形のまま。rgba(255,255,255,.22) が 1159 になる
return a === 0 ? [0,0,0,0] : [d[0]/a, d[1]/a, d[2]/a, a];
// ✅ 割らない
return a === 0 ? [0,0,0,0] : [d[0], d[1], d[2], a];
```

このリポジトリで実際に出た差。

| 対象 | 割り算あり（誤） | 割り算なし（正） |
|---|---:|---:|
| 半透明の白を敷いた `.menu-example`（青） | 2.48 | **1.78** |
| 同（緑） | 2.65 | **1.57** |
| 同（橙） | 2.69 | **1.54** |

**半透明の面に載せた文字を、実際より 0.7〜1.1 も良く見積もる。**
`rgba(255,255,255,.2)` を敷いたカード・チップ・バッジは
教育アプリで頻出なので、**同じひな形で測った全リポジトリに効く。**

### 3. 🔴 品質ゲートの共通検査（`giga-v4-checks.mjs`）に 3つの不具合

**直したものは `scripts/lib/giga-v4-checks.mjs` に入っている。正本にも反映が要る。**

| 不具合 | 中身 | だれに効くか |
|---|---|---|
| `src/` 決めうち | `walk(rootDir + '/src')`。A型は `js/` `css/` を直下に置くので**1バイトも読まれない** | **A型・D型の全リポジトリ**。`SAFE_AREA` `REDUCED_MOTION` `LOCALSTORAGE_CLEAR` `FLUID_TYPE` が空振りし、「0件」と出る |
| モーダル検出が Tailwind 決めうち | `"fixed inset-0"` だけを探す。素の CSS の `position: fixed; inset: 0` に当たらない | **Tailwind を使わない全リポジトリ**。このリポジトリはオーバーレイが5つあったのに 0件だった |
| 判定前にコメントを落としていない | §P4 は `SW_LOCALSTORAGE` の誤検知を挙げているが、同じ形が3箇所に残っていた | 全リポジトリ |

コメントの件は、**誤検知だけでなく見のがしの向きにも出る**。

| コード | 向き | 実際に起きたこと |
|---|---|---|
| `LOCALSTORAGE_CLEAR` | 誤検知 | 「`localStorage.clear()` も つかわない」という JSDoc に反応 |
| `CSP_INLINE_SCRIPT` | 誤検知 | HTML コメントの中の script 開きタグを数えていた |
| `PWA_INSTALL` | **見のがし** | コメントに `beforeinstallprompt` と書いてあるだけで通る。**捕捉をまるごと消しても 0件と言った** |

追加した設定キー（既定は従来どおりなので、既存リポジトリの結果は変わらない）。

```json
"gigaV4": { "sourceDirectories": ["js", "css"] }   // 既定は ["src"]
```

### 4. 🟡 正本（`project-quality.mjs`）側に 1つ

`extractScriptBlocks` が **HTML コメントを落とさない。**
コメントの中に script の開きタグを書くと、そこから次の閉じタグまでを
JavaScript とみなして構文検査し、`HTML_SCRIPT_SYNTAX` /
`ASSEMBLED_APP_SYNTAX` で落ちる。

正本は差し替えで受けられるようバイト単位のコピーのままにしてあるので、
このリポジトリでは `index.html` のコメントの書き方を変えて避けた。
**正本の側で直すこと。**

### 5. 🟡 「0件」を信じる前に、走査の範囲を疑う

最初の走査でコントラスト 53件・タップ 14件を見つけたが、
**データが無くて描画されない画面**（あしあと・バッジ）、
**`hidden` のままの UI**（さくせん選び・こたえを見る・けいさんの しかた）、
トースト、提示モード、1920px は測れていなかった。

localStorage に記録を仕込み、`hidden` を外して測り直したところ、
**タップ3件が新たに出た**（さくせん選びの2つ・インストールボタン）。

**「出ていない画面は 0件」になる。** それを ✅ と読んではいけない。

### 6. 🟡 タップ 44px は「疑似要素が 44px と報告される」だけでは足りない

`::after` の `min-height: 44px` を見て「44px ある」と数えるのは、
検査としては正しいが**押せる保証にはならない**。

**見た目の外側を実際にクリックして、反応することまで測る。**
このリポジトリでは、きろくのタブの見た目の 3px 外を押して
マップタブに切り替わることを確認した。

また、**縦にならぶボタンに疑似要素を当てるときは、すきまを先に測る。**
`.free-ops` は すきま 6px で 34px のボタンが2つ縦にならんでおり、
そのまま 44px に広げると 4px 重なる。すきまを 12px にしてから広げた。

---

## v5 の記述で、当ててみて差が出たところ

| §  | 書いてあること | 当ててみて |
|---|---|---|
| 7-2 | `getImageData` を α で割る | **Chrome では割ってはいけない**（上の 2） |
| 7-5 | `context.setOffline(true)` で圏外を測る | **SW の中の fetch には効かない**（上の 1） |
| P4 | 共通検査の不具合3件（`SW_CACHE_WIPE` / `SW_LOCALSTORAGE` / `VIEWPORT_100VH`） | **同じ形があと3件あった**（上の 3）。コメント落としは全検査に要る |

---

## 次にやること（Part V の引き継ぎに足すもの）

1. **`setOffline` で測った「オフライン確認ずみ」を全リポジトリで測り直す**
   … `offline.html` が無くても通っていた可能性がある
2. **§7-2 のひな形の割り算を外し、半透明の面を持つリポジトリを測り直す**
   … `rgba(255,255,255,.2)` を敷いたカード・チップ・バッジ
3. **`giga-v4-checks.mjs` の 3件を正本へ反映**
   … 特に `sourceDirectories`。A型・D型は現状ほぼ検査が空振りしている
4. **`project-quality.mjs` の `extractScriptBlocks` に HTML コメント落としを入れる**

```bash
# 3 の影響範囲：src/ を持たないリポジトリ（＝検査が空振りしている）
for r in */; do [ -d "$r/src" ] || echo "$r"; done
```
